# 공개 위키 작성 규칙

이 문서는 Obsidian에서 쓴 노트가 공개 사이트에 표시되는 방식을 설명한다. 속성명과 허용 값은 [[99_Templates/_property-schema|속성 스키마]]가 정본이다. 공개할 폴더와 파일은 이 프로젝트의 `config.json`에서 관리한다.

## 일반 링크와 글 카드

방문자용 소개 페이지의 원고는 사이트 저장소의 `src/content/about.md`에서 관리하고, `src/pages/about.astro`가 레이아웃을 담당한다. 이 원고는 일반 Markdown과 사이트 루트 기준 링크를 사용하며 vault의 위키링크·공개 노트 컬렉션에 포함되지 않는다. 노트 본문과 운영 원칙은 계속 vault에서 관리한다.

문장 안의 `[[노트 제목]]`은 일반 링크다. 이미지를 자동으로 붙이지 않는다.

다른 글을 별도 영역에서 소개하려면 아래처럼 `article` 콜아웃에 문서 링크 하나를 넣는다.

```markdown
> [!article]
> [[60일간의 AI 에이전틱 워크플로]]
```

연결 이유를 적으려면 콜아웃 제목을 사용한다. 사이트에서는 이 문구를 카드 제목 아래의 설명으로 표시하며, 대상 글의 요약은 함께 표시하지 않는다. 연결 이유가 없으면 대상 글의 요약을 사용한다.

```markdown
> [!article] 그 시행착오의 과정을 담은 지난 글
> [[60일간의 AI 에이전틱 워크플로]]
```

사이트는 빌드할 때 대상 문서의 실제 제목, 요약, `thumbnail`을 읽어 카드로 만든다. 표시 이름을 따로 쓴 링크도 카드에서는 대상 문서의 실제 제목을 사용한다. 썸네일이 없으면 텍스트 카드로 표시한다. 대상 글의 제목·요약·이미지를 수정한 뒤 다시 빌드하면 카드에도 반영된다.

카드 본문에는 링크 하나만 둔다. 문서 전체뿐 아니라 헤딩·블록 링크도 사용할 수 있다. 카드는 대상 문서의 제목·요약·썸네일을 보여주고, 클릭하면 지정한 절이나 문단으로 이동한다. 여러 링크, 추가 문장 또는 접기 표시(`+`/`-`)가 있으면 일반 콜아웃으로 표시한다. 코드 블록에 적은 예시는 카드로 바꾸지 않는다. `article`은 이 사이트의 표시 규칙이며, Obsidian에서는 기본 콜아웃과 링크로 읽을 수 있다.

하단에서 공개된 블로그 글을 소개할 때는 `## 연관된 글`에 같은 `article` 콜아웃을 모은다. 그 아래 `## 연관된 노트`에는 나머지 일반 링크를 두고, 항목이 없는 섹션은 만들지 않는다. 기존 연결 이유는 콜아웃 제목에 남기고, 특정 절·문단을 가리키던 링크는 그대로 유지한다. 본문 중간과 하단 모두 썸네일이 있으면 이미지 카드, 없으면 텍스트 카드로 표시한다. 일반 문장 속 링크와 공개되지 않은 자료의 링크는 바꾸지 않는다. 사이트가 이미 별도로 표시하는 연재의 이전·다음 글 내비게이션은 카드로 중복하지 않는다.

공개되지 않은 문서는 카드로 만들지 않는다. 실제 비공개 파일로 확인되는 링크에는 기존 자물쇠 표시를 사용하고, 존재를 확인할 수 없는 대상은 일반 텍스트로 남긴다. 비공개 문서의 제목 속성·요약·이미지를 가져오지 않는다.

## 연재 탐색과 관련 문서

연재 목차와 이전·다음 탐색은 `series`와 `series_order`로 만든다. 본문에 같은 안내 목록을 반복하지 않는다. 사이트는 본문에서 이전·다음 글 문구나 목록을 자동으로 제거하지 않는다.

`related` 목록에 둔 `"[[관련 글]]"`도 본문 링크와 함께 참조·역참조에 반영한다. 같은 대상은 한 번만 집계하며, 비공개 문서나 해석할 수 없는 대상은 출력하지 않는다. 속성의 링크를 본문에 자동 삽입하지는 않는다. 연재 순서는 `related`의 나열 순서와 무관하다.

전체 지도에는 기존 그래프 후보 규칙을 적용한다. `related`로 연결하더라도 지도에서 제외된 글을 지도 노드로 추가하지 않는다.

지도에서 노드를 선택하거나 이웃을 미리 볼 때는 제목 사이에 여백을 두고, 자리가 있는 제목부터 표시한다. 선택하거나 마우스를 올린 노트의 제목은 항상 표시하며, 연결 목록에서는 전체 제목을 확인할 수 있다.

노트 페이지 끝에는 구분선 하나 아래 한 줄에 목록 링크(왼쪽)와 사이트 소개 링크(오른쪽)를 둔다. 생각 노트는 사이드바의 지도 링크가 목록 역할을 하므로 소개 링크만 둔다. 모바일에서는 이 줄이 본문과 연결 목록 뒤에 온다. 모바일 지도의 상세 패널에서도 참조·역참조 목록을 볼 수 있다. 지도에 있는 노트를 목록에서 선택하면 패널이 해당 노트의 정보로 바뀌고, 지도 밖 문서는 해당 페이지로 이동한다.

본문의 실제 링크만 연결로 계산한다. 코드 블록·인라인 코드·주석·이스케이프된 예시와 운영 메모 속 링크는 참조·역참조나 지도 연결에 반영하지 않는다. 일반 인용문과 글 카드의 링크는 반영한다.

## 공개 본문과 자동 요약

사이트는 원문 사본에서 첫 제목과 Obsidian 주석, `운영 메모` 절을 제거한 뒤 본문·목차·검색 텍스트·자동 요약을 만든다. 운영 메모는 본문 흐름의 다음 헤딩까지 제거하며, 코드나 주석 속 헤딩은 절의 끝으로 보지 않는다. vault 원문은 수정하지 않는다.

명시한 `summary`를 우선한다. 요약이 없는 연재 허브는 공개 본문의 `연재 목적` 또는 `시리즈 소개` 절을 먼저 사용하고, 해당 절이 없으면 공개 본문에서 발췌한다. 외부 발행 글에는 본문 발췌 요약을 만들지 않는다.

## 주소와 slug

노트의 사이트 주소는 `/posts/<slug>/`, `/notes/<slug>/`, `/dev/<slug>/`다. frontmatter에 `slug`가 있으면 그 값을 쓰고, 없으면 제목에서 만든다. 제목에서 만들면 한글이 그대로 남아 주소에서 퍼센트 인코딩되고, 한 글자가 아홉 문자로 늘어난다.

새 공개 노트에는 `slug`를 처음부터 넣는다. 제목 문장을 그대로 옮기지 말고 핵심어 서너 개를 영문 소문자와 하이픈으로 적는다.

```yaml
slug: k8s-envfrom-secret-reload
```

글자·숫자·하이픈만 쓸 수 있고 다른 기호가 들어가면 빌드가 실패한다. 같은 종류 안에서 값이 겹쳐도 빌드가 실패하며 어느 파일끼리 겹쳤는지 알려준다. 이미 공개한 노트의 `slug`를 바꾸면 그 주소로 걸린 링크가 깨지므로 발행 전에 정한다.

## 날짜와 수정일

모든 공개 노트의 표시 날짜는 `published`가 있으면 그 값이고, 없으면 `created`다. 글의 `published`는 실제 발행일이고, 슬립박스와 개발 노트에서는 처음 공개하거나 승격한 날이다. `created`는 원본을 작성한 날로 유지한다. 상세 페이지의 메타데이터, 홈의 최근 목록, 개발 노트 목록과 OG 카드는 이 표시 날짜를 사용한다.

글 목록은 실제 발행일인 `published`를 기준으로 하며, 발행일이 없는 단독 글은 날짜별 목록 행에 넣지 않는다. 연재는 공개된 편 가운데 가장 늦은 `published`를 날짜로 쓰고, 편에 발행일이 하나도 없으면 편들의 표시 날짜 가운데 가장 늦은 날을 쓴다. 허브의 `last_published`와 `started`는 vault Base용 작성 필드라 사이트가 날짜로 쓰지 않는다. 외부 발행 글의 소개 화면에는 원문 발행일이 있을 때만 날짜를 표시한다.

모든 공개 노트에는 `created`가 있어야 한다. `published`와 `updated`는 선택 속성이므로 값 없이 두거나 `null`로 적어도 된다. 날짜를 적을 때는 `2026-09-11`처럼 달력에 있는 날을 `YYYY-MM-DD` 형식으로 쓴다. `created`가 없거나, 적힌 날짜의 형식이 틀렸거나 빌드한 날(한국 시간)보다 늦으면 빌드가 실패하고 파일과 필드를 알려준다. 미래 날짜를 예약 발행으로 처리하지 않으며, 공개 시점은 폴더와 `status`로 정한다. 날짜 오류로 빌드가 실패하면 사이트는 이전 배포로 남는다. vault를 고친 뒤 바로 반영하려면 배포 워크플로를 수동으로 실행한다(`gh workflow run deploy.yml`).

`updated`는 독자가 알아야 할 만큼 공개 본문을 고친 날이다. 오타를 고친 정도로는 바꾸지 않는다. 사이트는 이 값이 표시 날짜보다 늦을 때만 두 날짜를 함께 표시한다. 이때 첫 날짜는 `published`를 사용하면 글에는 "발행", 노트·개발 노트에는 "공개"를 붙이고, `created`를 사용하면 "작성"을 붙인다. 두 번째 날짜에는 "수정"을 붙인다. 수정일은 `article:modified_time`과 구조화 데이터의 `dateModified`로도 내보낸다. 같은 날이거나 더 이르면 표시하지 않는다. 외부 발행처에서 전문을 읽는 글에는 표시하지 않는다. 수정일은 정렬과 피드에 쓰지 않는다.

## 썸네일

썸네일은 대상 글의 frontmatter에 지정한다.

```yaml
thumbnail: "[[thumbnail-ai-harness-loop.jpg]]"
```

이미지의 위치와 스타일 값은 속성 스키마의 Blog Posts 규칙을 따른다. 썸네일에는 공개 대상으로 확인된 이미지 파일만 사용할 수 있다. 원본을 작은 파일로 덮어쓰지 않으며, 웹 빌드가 사용 위치에 맞는 크기와 형식으로 변환한다.

현재 썸네일을 사용하는 곳은 홈의 대표 글, 명시적인 글 카드, 외부 기고 소개 페이지, 글별 OG 공유 카드다. 목록과 전문을 공개하는 글의 본문 상단에는 자동으로 삽입하지 않는다. 전문을 공개하는 글에서는 본문 삽입 이미지를 별도로 유지한다. 같은 그림을 글 카드와 수동 이미지로 중복해서 넣지 않는다.

썸네일이 없는 글의 OG는 기존 노트 그래프를 사용한다. 사이트 자체를 공유할 때의 OG는 전체 지도다.

## 본문 이미지 캡션

문단에 이미지 하나만 있고 바로 다음 문단을 통째로 기울임(`*캡션*`)으로 쓰면 그 문단이 그림의 캡션이 된다. Obsidian에서도 기울임 문단으로 보여 원문과 사이트의 모양이 같다. 문장 속 이미지, 링크로 감싼 이미지, 캡션 없는 이미지는 그대로 표시한다. 기울임으로 시작만 하거나 끝만 기울임인 문단은 캡션이 아니라 보통 문단이다. 대체 텍스트는 그림의 내용을 적고, 캡션에는 읽는 사람에게 할 말을 적는다.

## 외부 발행처에서 전문을 읽는 글

`config.json`의 `externalPublications`에 지정한 발행처는 가든에 짧은 소개 페이지만 남긴다. 현재 넥스트리 기술 블로그가 해당한다. 기존 주소와 공개 노트 연결은 유지하고, 전문은 원문 버튼으로 이동해 읽는다.

소개 페이지에는 제목, 명시한 `summary`, 발행처·발행일, 원문 링크와 지정한 썸네일을 표시한다. 썸네일은 제목과 요약 사이에 원래 비율로 배치하고, 높이를 제한해 읽기 흐름을 유지한다. 썸네일이 없는 페이지에는 빈 이미지 영역을 만들지 않는다. 요약이 비어 있어도 본문에서 발췌하지 않는다.

해당 글의 전문, 목차, 읽기 시간, 본문 속 글 카드와 본문 전용 이미지는 웹 빌드에 내보내지 않는다. 검색에도 본문을 넣지 않는다. 이 규칙은 웹사이트 출력에만 적용하며 원본 Markdown과 Git 이력을 바꾸지 않는다.

다른 공개 글에서 이 글을 `[!article]`로 소개하면 제목·요약·썸네일을 표시하고 가든의 소개 페이지로 연결한다. 발행처를 추가하거나 전문 공개로 되돌릴 때는 이 정책과 출력 검사를 함께 검토한다.

## 피드

피드는 네 개다. `rss.xml`은 글·노트·개발 노트를 합친 통합 피드이고, `feeds/posts.xml`, `feeds/notes.xml`, `feeds/dev.xml`은 종류별 피드다. 종류별 피드는 최근 30편을 담는다. 통합 피드는 종류마다 최근 10편씩 뽑아 합친다. 글은 발행일, 노트는 기록일이라 시간축이 달라서, 날짜순으로만 합치면 최근 노트가 글을 밀어내기 때문이다. 전체 목록은 사이트가 맡고 피드는 새 항목을 알리는 통로다.

항목은 최신 날짜순이고, 같은 날이면 항목 주소(guid) 순서다. 제목은 고칠 수 있는 표시 값이라 정렬 기준으로 쓰지 않는다. 항목 주소는 canonical과 같은 가든 주소다. 외부 발행처에서만 전문을 읽는 글(`externalPublications`에 해당)만 원문 주소로 연결한다. 가든에 전문이 있는 글은 외부에도 발행했더라도 가든 주소로 나간다.

글은 `published` 날짜가 있어야 피드에 들어가고, 노트와 개발 노트는 위에서 정한 표시 날짜를 쓴다. 허브(MOC)와 연재 허브처럼 다른 노트를 묶어 안내하는 노트는 넣지 않는다. 만든 날이 새 생각이 생긴 날이 아니기 때문이다. 홈의 최근 기록도 같은 규칙을 따른다. 모든 페이지가 네 피드를 `<link rel="alternate">`로 알려서 피드 리더가 통합·종류별 중 고를 수 있다.

## 규칙을 변경할 때

- frontmatter 속성을 바꾸면 속성 스키마를 수정한다.
- 새로운 작성 문법이나 표시 조건을 추가하면 이 문서와 관련 테스트를 함께 수정한다.
- 공개 범위를 바꾸려면 `config.json`의 게시 규칙과 이미지 허용 목록을 확인한다. 링크나 카드 지정만으로 비공개 자료를 공개하지 않는다.

구현 위치는 `src/lib/markdown.ts`(문법), `src/lib/slug.ts`(주소), `src/lib/garden.ts`(공개 문서 조립), `src/lib/links.ts`(위키 링크 해석), `src/lib/note-body.ts`(공개 본문과 요약), `src/lib/public-assets.ts`(본문·썸네일에 쓸 수 있는 이미지), `src/components/ArticleBody.astro`와 `src/lib/article-card.ts`(카드 출력), `src/styles/article-card.css`(배치), `src/styles/body.css`(콜아웃·표·코드 블록 같은 본문 요소의 모양), `src/lib/og.ts`(공유 카드), `src/lib/rss.ts`(피드)다.
