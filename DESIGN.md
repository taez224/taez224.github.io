---
version: alpha
name: TaeZ's Thinking Garden
description: 한국어 글을 읽고 지도와 참조 관계로 관련 노트를 탐색하는 개인 위키
colors:
  primary: '#252e29'
  paper: '#f7f7f2'
  paper-strong: '#fdfdfa'
  muted: '#626d64'
  line: '#d8ded4'
  accent-soft: '#ecede8'
  warning: '#985d2f'
  bug: '#8e5d5d'
  highlight: '#e7d99b'
  primary-dark: '#e1e8e3'
  paper-dark: '#151b17'
  paper-strong-dark: '#1c221e'
  muted-dark: '#89948c'
  line-dark: '#353d37'
  accent-soft-dark: '#242b26'
  warning-dark: '#b37d51'
  bug-dark: '#ae7e7d'
  highlight-dark: '#574d24'
  code-bg: '#ffffff'
  code-text: '#24292f'
  code-bg-dark: '#1c221e'
  code-text-dark: '#e6edf3'
typography:
  title:
    fontFamily: Gowun Batang
    fontSize: 46px
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: -0.03em
  title-mobile:
    fontFamily: Gowun Batang
    fontSize: 32px
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: -0.03em
  hero:
    fontFamily: Gowun Batang
    fontSize: 32px
    fontWeight: 700
    lineHeight: 1.45
    letterSpacing: -0.02em
  featured:
    fontFamily: Gowun Batang
    fontSize: 34px
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: -0.02em
  featured-tablet:
    fontFamily: Gowun Batang
    fontSize: 28px
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: -0.02em
  featured-mobile:
    fontFamily: Gowun Batang
    fontSize: 24px
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: -0.02em
  page:
    fontFamily: Gowun Batang
    fontSize: 28px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  section:
    fontFamily: Gowun Batang
    fontSize: 24px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  section-mobile:
    fontFamily: Gowun Batang
    fontSize: 20px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  h2:
    fontFamily: Gowun Batang
    fontSize: 26px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  h2-mobile:
    fontFamily: Gowun Batang
    fontSize: 22px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  h3:
    fontFamily: Gowun Batang
    fontSize: 21px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  h3-mobile:
    fontFamily: Gowun Batang
    fontSize: 19px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  h4:
    fontFamily: Gowun Batang
    fontSize: 17px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  lead:
    fontFamily: Gowun Batang
    fontSize: 18.5px
    fontWeight: 400
    lineHeight: 1.65
    letterSpacing: -0.01em
  ledger:
    fontFamily: Gowun Batang
    fontSize: 40px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  ledger-mobile:
    fontFamily: Gowun Batang
    fontSize: 30px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  ledger-year:
    fontFamily: Gowun Batang
    fontSize: 32px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  ledger-year-mobile:
    fontFamily: Gowun Batang
    fontSize: 24px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  ledger-word:
    fontFamily: Gowun Batang
    fontSize: 18px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  ledger-word-mobile:
    fontFamily: Gowun Batang
    fontSize: 17px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: -0.02em
  wordmark:
    fontFamily: Gowun Batang
    fontSize: 22px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  wordmark-mobile:
    fontFamily: Gowun Batang
    fontSize: 20px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.02em
  base:
    fontFamily: Pretendard Variable
    fontSize: 17px
    fontWeight: 400
    lineHeight: 1.85
  base-mobile:
    fontFamily: Pretendard Variable
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.85
  body:
    fontFamily: Pretendard Variable
    fontSize: 17px
    fontWeight: 400
    lineHeight: 1.85
  summary:
    fontFamily: Pretendard Variable
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.75
  summary-mobile:
    fontFamily: Pretendard Variable
    fontSize: 15px
    fontWeight: 400
    lineHeight: 1.75
  list:
    fontFamily: Pretendard Variable
    fontSize: 20px
    fontWeight: 600
    lineHeight: 1.45
  list-mobile:
    fontFamily: Pretendard Variable
    fontSize: 18px
    fontWeight: 600
    lineHeight: 1.45
  list-small:
    fontFamily: Pretendard Variable
    fontSize: 17px
    fontWeight: 600
    lineHeight: 1.55
  ui:
    fontFamily: Pretendard Variable
    fontSize: 15px
    fontWeight: 600
    lineHeight: 1.5
  ui-small:
    fontFamily: Pretendard Variable
    fontSize: 14px
    fontWeight: 600
    lineHeight: 1.2
  side:
    fontFamily: Pretendard Variable
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.5
  meta:
    fontFamily: Pretendard Variable
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.6
  micro:
    fontFamily: Pretendard Variable
    fontSize: 12px
    fontWeight: 400
    lineHeight: 1.5
  callout-title:
    fontFamily: Pretendard Variable
    fontSize: 13px
    fontWeight: 600
    lineHeight: 1.5
    letterSpacing: 0.04em
  code:
    fontFamily: ui-monospace
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.6
spacing:
  content-max: 1180px
  page-gutter: 24px
  page-gutter-mobile: 20px
  column-gap: 40px
  reader-column: 720px
  sidebar: 310px
  paragraph-gap: 22px
  side-list-gap: 8px
  header-height: 72px
  header-height-mobile: 56px
  title-gap: 40px
  title-gap-mobile: 28px
  page-top-index: 36px
  page-top-index-mobile: 24px
  page-top-reading: 56px
  page-top-reading-mobile: 32px
  page-bottom: 96px
  page-bottom-mobile: 64px
rounded:
  none: 0px
  control: 4px
  overlay: 6px
components:
  article:
    backgroundColor: '{colors.paper}'
    textColor: '{colors.primary}'
    typography: '{typography.body}'
  button-primary:
    backgroundColor: '{colors.primary}'
    textColor: '{colors.paper-strong}'
    typography: '{typography.ui}'
    rounded: '{rounded.control}'
    padding: '0 20px'
    height: 44px
  button-primary-small:
    backgroundColor: '{colors.primary}'
    textColor: '{colors.paper-strong}'
    typography: '{typography.ui-small}'
    rounded: '{rounded.control}'
    padding: '0 18px'
    height: 40px
  button-primary-hover:
    backgroundColor: '{colors.muted}'
    textColor: '{colors.paper-strong}'
  header-icon-button:
    backgroundColor: '{colors.paper}'
    textColor: '{colors.muted}'
    rounded: '{rounded.control}'
  header-icon-button-hover:
    backgroundColor: '{colors.accent-soft}'
    textColor: '{colors.primary}'
  meta-line:
    backgroundColor: '{colors.paper}'
    textColor: '{colors.muted}'
    typography: '{typography.meta}'
  count:
    backgroundColor: '{colors.paper}'
    textColor: '{colors.muted}'
    typography: '{typography.meta}'
  series-link:
    backgroundColor: '{colors.paper}'
    textColor: '{colors.primary}'
    rounded: '{rounded.control}'
    padding: '14px 18px'
  series-link-hover:
    backgroundColor: '{colors.accent-soft}'
    textColor: '{colors.primary}'
  selected-control:
    backgroundColor: '{colors.line}'
    textColor: '{colors.primary}'
    typography: '{typography.meta}'
    rounded: '{rounded.control}'
  hover-meta:
    backgroundColor: '{colors.accent-soft}'
    textColor: '{colors.muted}'
    typography: '{typography.meta}'
  callout:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.primary}'
    typography: '{typography.summary}'
    rounded: '{rounded.none}'
    padding: '12px 18px 16px'
  callout-title:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.muted}'
    typography: '{typography.callout-title}'
  callout-rule:
    backgroundColor: '{colors.primary}'
    rounded: '{rounded.none}'
    height: 2px
  callout-warning-title:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.warning}'
    typography: '{typography.callout-title}'
  callout-bug-title:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.bug}'
    typography: '{typography.callout-title}'
  code-block:
    backgroundColor: '{colors.code-bg}'
    textColor: '{colors.code-text}'
    typography: '{typography.code}'
    rounded: '{rounded.none}'
    padding: '14px 16px'
  code-block-dark:
    backgroundColor: '{colors.code-bg-dark}'
    textColor: '{colors.code-text-dark}'
    typography: '{typography.code}'
    rounded: '{rounded.none}'
    padding: '14px 16px'
  text-highlight:
    backgroundColor: '{colors.highlight}'
    textColor: '{colors.primary}'
  search-dialog:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.primary}'
    rounded: '{rounded.overlay}'
  search-dialog-empty:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.muted}'
  article-dark:
    backgroundColor: '{colors.paper-dark}'
    textColor: '{colors.primary-dark}'
    typography: '{typography.body}'
  meta-line-dark:
    backgroundColor: '{colors.paper-dark}'
    textColor: '{colors.muted-dark}'
    typography: '{typography.meta}'
  hover-meta-dark:
    backgroundColor: '{colors.accent-soft-dark}'
    textColor: '{colors.muted-dark}'
    typography: '{typography.meta}'
  selected-control-dark:
    backgroundColor: '{colors.line-dark}'
    textColor: '{colors.primary-dark}'
    typography: '{typography.meta}'
    rounded: '{rounded.control}'
  button-primary-dark:
    backgroundColor: '{colors.primary-dark}'
    textColor: '{colors.paper-strong-dark}'
    typography: '{typography.ui}'
    rounded: '{rounded.control}'
  button-primary-hover-dark:
    backgroundColor: '{colors.muted-dark}'
    textColor: '{colors.paper-strong-dark}'
  callout-title-dark:
    backgroundColor: '{colors.paper-strong-dark}'
    textColor: '{colors.muted-dark}'
    typography: '{typography.callout-title}'
  callout-warning-title-dark:
    backgroundColor: '{colors.paper-strong-dark}'
    textColor: '{colors.warning-dark}'
    typography: '{typography.callout-title}'
  callout-bug-title-dark:
    backgroundColor: '{colors.paper-strong-dark}'
    textColor: '{colors.bug-dark}'
    typography: '{typography.callout-title}'
  text-highlight-dark:
    backgroundColor: '{colors.highlight-dark}'
    textColor: '{colors.primary-dark}'
  map-sheet:
    backgroundColor: '{colors.paper-strong}'
    textColor: '{colors.primary}'
    rounded: '{rounded.none}'
  divider:
    backgroundColor: '{colors.line}'
    rounded: '{rounded.none}'
    height: 1px
---

## Overview

Thinking Garden은 한 소프트웨어 엔지니어가 글과 개발 노트, 읽은 책을 모아 두는 개인 위키다. 독자는 검색이나 링크로 들어와 한 편을 읽고, 참조 관계를 따라 다른 노트로 넘어간다. 화면은 인쇄된 기록장과 개인 서재의 장부를 기준으로 삼는다. 첫인상을 만드는 장식보다 긴 글의 읽기 흐름과 노트 사이의 관계를 앞세우고, 마케팅 랜딩 페이지나 데이터 대시보드처럼 한 화면을 정보로 채우지 않는다.

홈에서는 노트 지도로 주제와 연결을 살펴보고, 목록에서는 제목과 요약으로 글을 고르며, 리더에서는 본문을 읽다가 참조·역참조를 따라 이동한다.

**핵심 특징:**

- 먹색 한 가지로 글자와 조작을 그리고, 색은 주제와 상태를 알릴 때만 쓴다.
- 목록은 왼쪽 여백에 큰 라벨을 둔 장부 격자다.
- 콜아웃은 인쇄물의 박스 주석처럼 밝은 판과 위 괘선으로 본문에서 떼어 낸다.
- 그림자 없이 구분선, 판의 밝기, 여백으로 층을 나눈다.
- 밝은 화면과 어두운 화면을 따로 짜고, 어느 쪽을 보일지는 시스템 설정이 정한다.

## Colors

색은 먹과 종이 한 쌍이 거의 전부이고, 색을 띠는 것은 주제와 상태 표시뿐이다.

**먹 한 가지 규칙.** 글자, 링크, 버튼, 선택 표시, 포커스 윤곽선은 모두 먹색이다. 링크는 파랑으로 칠하지 않고 밑줄로 구분한다. 색을 쓰는 곳은 주제(지도·태그 점·목차 위치)와 상태(경고·버그·형광)뿐이다.

**보조색 한 단계 규칙.** 보조 글자색은 `muted` 하나다. 종이색 위에서 4.5:1을 지키는 가장 옅은 회색이 `muted`와 거의 같아서, 개수나 단축키처럼 더 낮은 위계는 색을 옅게 하지 않고 글자 크기로 나눈다.

### 역할 색

- **먹**(`primary`): 글자, 링크, 채운 버튼, 선택 표시, 포커스 윤곽선.
- **종이**(`paper`): 페이지 바탕.
- **밝은 종이**(`paper-strong`): 페이지보다 한 단계 올라온 판. 표 머리, 콜아웃, 도표 상자, 대화상자, 지도 시트.
- **보조**(`muted`): 날짜·개수·출처 같은 보조 정보.
- **구분선**(`line`): 정보 덩어리 사이의 1px 선. 종이색과 1.28:1이라 밑줄이나 선택 표시로는 쓰지 않는다.
- **호버 판**(`accent-soft`): 투명 버튼의 호버 바탕. 그 위에 보조 글자가 올라가므로 보조색이 4.5:1을 넘는 가장 짙은 값이다. 종이색과 1.10:1이라 선택 표시로는 쓰지 않는다.
- **경고**(`warning`)·**버그**(`bug`): 경고·위험·실패와 버그 콜아웃의 위 괘선·머리표·아이콘.
- **형광**(`highlight`): 본문의 형광 표시.
- **간선**(`edge`): 지도의 연결선. 낮은 불투명도로 그리므로 구분선보다 짙은 값에서 출발한다. 밝은 화면 `#9aab9d`, 어두운 화면 `#616c64`다. 면이 아니라 선에만 쓰는 색이라 문서 앞머리의 `colors` 목록에는 넣지 않는다. 넣으면 어느 컴포넌트도 참조하지 않아 lint가 쓰이지 않는 토큰으로 경고한다.

### 링크와 선택

링크 밑줄은 먹색 35%(종이색에서 2.02:1)다. 본문 링크, 홀로 선 링크(개발 노트 분류, 홈의 지도 보기·소개 링크, 장부 행의 1편부터 읽기), 글자 버튼이 같은 밑줄을 쓰고, 호버에서 밑줄을 먹색으로 채운다. 장부 행처럼 행 전체가 이미 다른 링크인 자리에서는 색만으로 무엇이 따로 눌리는지 알 수 없다. 주변 글과 2.59:1뿐이기 때문이다. 다만 밖으로 나가는 링크는 ↗가 이미 그 일을 하므로 밑줄을 겹치지 않는다. 글자를 선택하면 먹색 15% 판이 깔린다. 형광은 본문의 강조 표시와 구분되지 않아 선택 색으로 쓰지 않는다. 먹색의 반투명 변형(인라인 코드 바탕, 링크 밑줄, 선택 색, 떠 있는 판의 테두리)은 먹색에서 섞어 만들어 값을 따로 적지 않는다.

### 주제색

주제색은 지도의 노드·영역·영역 이름, 태그 점, 목차의 현재 위치에만 쓴다. 색과 함께 주제 이름, 허브 링, 연결 목록을 두어 색을 가리지 못해도 뜻을 읽을 수 있다. 개발은 하나로 합친 주제색을 쓰고, 버그와 글쓰기는 같은 색상 계열을 나누어 쓰되 용도로 구분한다.

주제마다 색상은 유지하고 명도를 나눠, 적색약·녹색약에서도 지도에 보이는 주제끼리 붙지 않게 한다. 노트가 많은 AI·지식관리·조직은 지도가 무거워지지 않게 중간 톤에 두고 나머지 주제의 명도를 벌린다. 기타는 무채색이다. 노드는 바탕 위 3:1 이상이다. 영역 이름은 15px 굵은 글자라 4.5:1이 필요하므로, 노드 색이 이 기준에 못 미치는 주제는 같은 색상에서 명도만 낮춘 글자색을 쓴다.

어두운 화면의 주제색은 모두 같은 대비로 맞추면 명도 차이가 사라져 색각 이상에서 이웃 주제가 붙는다. 그래서 주제마다 밝기 단계를 달리해 골랐고, 노드는 3.3:1, 영역 이름은 5:1 이상이다.

| 주제 | 노드 | 영역 이름 | 어두운 노드 | 어두운 영역 이름 |
|---|---|---|---|---|
| AI | `#877096` | `#7e678d` | `#7c628d` | `#9a7fac` |
| 개발 | `#405a78` | 노드와 같음 | `#6686ab` | `#6c8cb1` |
| 커리어 | `#8c6a45` | `#8a6944` | `#8b6438` | `#ab8256` |
| 지식관리 | `#5a7e6a` | `#547864` | `#6e9981` | 노드와 같음 |
| 글쓰기 | `#7c5050` | 노드와 같음 | `#925e5e` | `#b27b7b` |
| 철학 | `#94833e` | `#7f6e28` | `#96822d` | `#9d8936` |
| 조직 | `#36737d` | 노드와 같음 | `#5ea6b2` | 노드와 같음 |
| 심리 | `#793a58` | 노드와 같음 | `#d483a8` | 노드와 같음 |
| 기타 | `#868684` | `#70706e` | `#787876` | `#8a8a88` |

### 어두운 화면

**시스템 설정 규칙.** 어두운 화면은 운영체제·브라우저 설정을 따르고, 사이트 안에 전환 버튼은 두지 않는다. 헤더에 자리가 없고, 선택을 기억하는 스크립트가 없으니 첫 화면부터 맞는 색으로 그려진다.

어두운 팔레트(`-dark` 토큰)는 밝은 팔레트를 뒤집지 않고 먹색과 같은 녹회색 색조로 따로 짰다. 바탕보다 올라온 판일수록 밝아서 그림자 없이도 층이 보인다. 글자는 흰색 대신 옅은 먹색이라 본문 대비가 14:1이고, 보조 글자는 가장 밝은 판 위에서도 4.6:1이다. 역할은 밝은 화면과 같고, 따로 정한 것은 다음과 같다.

- 페이지를 덮는 뒤 배경은 먹색이 밝아지므로 검정 반투명을 쓴다. 값은 Elevation & Depth에 있다.
- 지도의 주제 영역 색면은 바탕에 묻히지 않게 불투명도를 .06에서 .1로 올린다.
- 검은 글리프인 GitHub·Brunch 로고는 색을 뒤집는다.
- 대표 글·연재·외부 발행 글·글 카드의 썸네일은 흰 바탕 그림이 밝은 덩어리로 뜨므로 밝기를 85%로 낮춘다. 색 뒤집기는 포스터와 사진을 망가뜨려 쓰지 않고, 본문 안 그림과 책 표지는 그대로 둔다.
- 코드 강조와 도표는 각자의 어두운 색을 쓴다. Components의 코드 블록과 도표에 있다.
- OG 카드는 밝은 화면 색만 쓴다.

## Typography

Gowun Batang은 제목과 요약·인용에, Pretendard Variable은 본문·목록·조작 요소에 쓴다. 명조는 제목에서 700으로, 요약·인용에서 400으로 두어 제목보다 한 단계 물러나게 한다. 두 글꼴은 외부 CDN 대신 자체 호스팅한다. 한국어 글꼴이 글자 범위별 조각으로 나뉘어 있어 페이지에 쓰인 글자의 조각만 받는다. 명조에 새 굵기를 쓰려면 글꼴을 받는 스크립트의 목록에도 더한다. 한 굵기만 받아 두면 브라우저가 다른 굵기 요청을 그 파일로 대신 그리기 때문이다. 코드는 시스템 고정폭 서체(ui-monospace, SFMono-Regular, Menlo)를 쓴다. 긴 한국어 문장은 단어 단위로 묶어 줄바꿈하고, 긴 URL은 가용 폭에 맞춰 끊는다.

**역할 토큰 규칙.** 글자 크기는 역할 토큰으로만 정한다. 토큰은 rem이라 브라우저의 기본 글자 크기 설정을 따르고, 폭에 따라 달라지는 역할은 `-mobile`(720px 이하)·`-tablet`(721~1000px) 토큰에서 값만 바꾼다. 그래서 컴포넌트에는 글자 크기용 미디어 쿼리가 없고 단계 전체가 함께 줄어든다. 예외는 엔진이 px로 배치를 계산하는 그래프 SVG의 이름 글자뿐이다.

**세 굵기 규칙.** 굵기는 400·600·700만 쓴다.

### 위계

- **제목:** `title`(글 제목, 리더·소개), `hero`(홈 소개 제목), `featured`(대표 글), `page`(목록 페이지), `section`(홈 절 제목·지도 패널), `ledger`·`ledger-year`·`ledger-word`(장부 라벨), 본문의 `h2`·`h3`·`h4`. 모바일에서는 함께 줄여 글 제목과 h2, 페이지 제목과 장부 연도, 절 제목과 대표 글 제목이 같은 크기로 겹치지 않게 한다.
- **읽는 글자:** `body`(본문), `lead`(제목 아래 요약, 요약·인용 콜아웃 본문), `summary`(목록 요약·콜아웃 본문). 페이지 기본 글자 `base`는 모바일에서 16px로 줄지만 본문 `body`는 17px을 유지한다.
- **조작과 보조 정보:** `list`·`list-small`(목록 제목), `ui`(내비·버튼·검색 결과, 크기만 공유한다. 채운 버튼은 44px 상자 안에서 가운데 맞추려고 줄 간격을 1.2로 두고, 헤더 메뉴는 본문 굵기를 그대로 쓴다), `side`(사이드바·표·코드), `meta`, `micro`(단축키·출처처럼 작은 표기), `callout-title`(콜아웃 머리표. 크기 토큰을 따로 두지 않고 `meta` 크기에 굵기와 자간을 더해 만든다).

리더 제목과 홈 대표 글·연재 제목, 소개 페이지 제목은 줄 길이를 고르게 나눠 마지막 줄에 한 단어만 남는 것을 줄인다. 새 역할이 생기면 비슷한 크기의 기존 토큰에 합칠 수 있는지 먼저 보고, 합칠 수 없을 때만 토큰을 더한다.

## Layout

페이지 틀은 공통 폭(`content-max`), 좌우 여백(`page-gutter`), 리더 열(`reader-column`), 사이드바(`sidebar`), 열 간격(`column-gap`)으로 짠다. 토큰으로 묶는 것은 페이지 틀(폭, 좌우 여백, 열, 헤더, 페이지 위아래 여백, 제목 아래 간격, 문단 간격, 사이드 목록 간격)뿐이다. 컴포넌트 안쪽의 작은 여백과 장부 격자의 24px·36px 간격은 모양에 맞춘 값이라 묶지 않는다. 열 간격은 리더 본문과 사이드바, 지도와 패널, 홈의 안내 열과 콘텐츠 사이에 쓴다. 모바일 값은 `-mobile` 토큰에 있다.

**여백 두 단계 규칙.** 헤더 아래 첫 요소까지의 여백은 목록을 보여 주는 페이지(글·개발 노트·책장·지도)가 `page-top-index`, 읽는 페이지(리더·소개·외부 발행 글·찾을 수 없는 페이지)가 `page-top-reading`이다. 같은 종류의 페이지를 오갈 때 제목이 같은 높이에서 시작한다.

**구조에 맞춘 정렬 규칙.** 오른쪽에 사이드바가 있는 두 열 페이지(목록, 리더)는 워드마크와 같은 선에서 시작하고, 사이드바가 없는 한 열 페이지(소개, 외부 발행 글)는 리더 열을 가운데에 둔다. 한 열 페이지를 왼쪽에 붙이면 넓은 화면에서 오른쪽이 크게 비어 미완성처럼 보인다. 리더에서 소개로 넘어갈 때 본문 시작선이 옮겨 가지만, 두 페이지를 오가는 일이 드물어 구조에 맞는 배치를 우선한다.

### 전환 폭

720px(모바일)과 1000px(한 열)이 기본이다. 그 밖에는 홈 소개의 1240px, 책장의 480px, 좁은 헤더에서 공유 아이콘을 숨기는 359/360px, 글자를 키웠을 때 헤더를 두 줄로 나누는 19.5em만 쓴다. 19.5em만 글자 크기를 따라 움직이는 경계다.

- **1000px 이하:** 한 열로 바뀌고 참조·역참조가 본문 뒤로 간다. 지도 패널은 하단 시트로 바뀐다.
- **720px 이하:** 좌우 여백과 헤더 높이가 모바일 값이 되고, 목록의 열이 세로로 쌓이며, 목차는 접힘으로 바뀐다. 긴 표와 코드는 블록 안에서 가로로 스크롤한다.

### 페이지별 배치

- **지도:** 무대 높이는 화면 높이에서 고정된 머리 높이와 `page-top-index`를 빼서 정한다. 주제 필터는 모바일에서 한 줄로 스크롤한다. 제목 옆 집계는 필터가 남긴 범위를 말하고, 선택 때문에 움직이지 않는다. 이 줄은 생중계 영역이라 필터를 누르면 달라진 수가 낭독기에도 전해진다. 한 열로 바뀌는 1000px 이하에서는 패널을 하단 시트로 띄운다. 흐름에 두면 무대 아래로 내려가 노드를 골라도 화면에서 바뀌는 것이 없다. 이 문제는 입력 방식이 아니라 한 열로 접히는 데서 오므로 경계는 폭으로만 정한다. 시트가 지도를 덮으므로 포커스는 시트 안에 가둔다. 시트는 노드를 고른 뒤에야 올라오므로, 패널이 처음에 보여 주던 시작점·허브 목록은 무대 아래에 두고 어디서 열리는지 한 줄로 알린다.
- **홈 첫 화면:** 첫 화면의 높이는 기준이지 한계가 아니다. 소개 블록이 흐름 안에 있어 글자를 키우면 상자가 함께 커지고, 지도와 종이색 그라데이션은 절대 배치라 그 높이를 따라간다. 높이를 못 박고 넘치는 부분을 잘라 내면 글자 크기를 키워 쓰는 독자가 링크와 연락처를 잃는다. 소개 열의 왼쪽은 본문 열의 왼쪽 끝을 따라가되 좌우 여백 아래로는 내려가지 않는다. 모바일에서는 첫 글까지의 이동을 줄이려고 소개 위 여백을 20px로 둔다. 이 폭의 지도는 정적 그림이라 허브 제목이 7px 남짓으로 줄어들므로, 허브 제목을 숨기고 영역 이름만 크게(화면 약 17px) 놓은 그림으로 바꾼다. 큰 이름이 그래프 한가운데로 밀려나지 않게 그림 위쪽 여백까지 이름 자리로 쓰고, 그 여백만큼 그림 상자를 높인다(`clamp(230px, 78vw, 350px)`). 넓은 화면에서 연 페이지를 이 폭으로 줄이거나 기기를 세로로 돌려도 같은 그림으로 돌아온다. 손가락으로 쓰는 기기는 폭과 상관없이 이 그림을 받는다. 살아 있는 지도는 세로 스와이프를 가져가고, 그 대가로 얻는 호버 미리보기는 손가락에 없다. 넓은 폭 안에서 창 크기가 바뀌면 살아 있는 지도를 새 상자에 다시 맞춰 노드 제목 크기를 유지한다. 소셜 링크는 데스크톱 홈과 소개 페이지에 둔다. 소개문 아래에는 채운 버튼 대신 생각 지도와 소개 페이지로 가는 보조 링크 둘을 한 줄에 나란히 둔다. 휴대폰 폭의 그림은 개별 노드를 누를 수 없으므로 그림 전체를 지도로 가는 링크로 두고, 그림 아래에는 따로 링크를 두지 않는다. 소개문 아래의 링크는 넓은 화면에서 그림이 살아 있는 지도로 바뀌어 지도 자체로 가는 길이 사라지기 때문에 둔다.
- **홈 절 격자:** 대표 글·최근 기록·최근 연재는 왼쪽 120px 안내 열과 오른쪽 콘텐츠를 열 간격으로 맞추고, 721~1000px에서는 안내 열을 100px로 줄인다. 절 사이는 전체 너비의 선 하나로 나눈다.
- **대표 글:** 왼쪽에 절 제목·날짜·발행처를, 오른쪽에 글 제목·명시한 요약·썸네일을 둔다.
- **최근 기록:** 종류마다 한 편씩 날짜·종류·제목·주제 순으로 보이고 건수는 적지 않는다. 721~1000px에서는 주제를 제목 아래에, 720px 이하에서는 날짜·종류·주제를 제목 아래에 둔다. 행 구분선은 항목 사이에만 둔다.
- **찾을 수 없는 페이지:** 제목 위에 한 칸 6px의 그림을 하나 둔다. 참조로 이어지던 길이 끊기고 건너편 자리가 비어 있는 모습이다. 노트를 점으로, 참조를 선으로 두는 지도의 구조를 따르되 모양은 격자에 맞춘 네모라 지도의 둥근 노드와는 다르다. 장식이 아니라 상태 설명이므로 보조색으로 두어 제목과 세 갈래 길(지도, 검색, 홈)이 먼저 읽히게 한다. 빈 검색 결과에는 두지 않는다.
- **최근 연재:** 허브 썸네일이 있으면 대표 글과 같은 이미지 열을 쓰고, 제목은 목록 제목 크기로 대표 글보다 낮은 위계에 둔다. 절 제목 아래에는 공개된 편수와 진행 상태만 적고 날짜는 넣지 않는다.

## Elevation & Depth

**그림자 없음 규칙.** 페이지의 판과 조작 요소에는 그림자를 쓰지 않는다. 층은 구분선, 판의 밝기, 여백, 그리고 겹치는 판의 테두리와 뒤 배경으로 나눈다. 뒤 배경이 이미 층을 가르므로 그림자는 같은 단서를 한 번 더 주는 셈이고, 가는 테두리와 넓은 그림자를 함께 쓰면 경계가 흐려진다.

- **떠 있는 판**(검색 대화상자, 각주 판, 공유 실패 안내): 경계가 보여야 하므로 먹색 28% 테두리를 둔다. 검색 대화상자 뒤에는 먹색 32% 배경을 깔고, 어두운 화면에서는 검정 50%를 쓴다.
- **화면을 덮는 판**(도표 크게 보기, 지도 시트): 판의 경계가 곧 화면 끝이라 구분선 색 테두리만 둔다. 뒤 배경은 도표 보기가 먹색 55%(어두운 화면은 검정 70%), 지도 시트가 먹색 25%(검정 45%)다.

지도의 옅은 주제 영역은 노드의 묶음을 보여 준다. 도표 노드에만 도표 도구가 그리는 옅은 그림자가 있어 바탕과 도형을 구분한다. 긴 목록과 가로 스크롤 필터의 끝에는 페이드를 두어 화면 밖에 내용이 더 있음을 알린다.

## Shapes

**세 모서리 규칙.** 모서리는 직각(`none`), 조작 요소(`control`, 4px), 떠 있는 판(`overlay`, 6px) 세 가지다. 본문의 표·코드·콜아웃은 직각이고, 버튼·연재 탐색 카드·비공개 표시 말풍선은 4px, 검색 대화상자·각주 판·공유 안내·지도 확대 컨트롤과 키 안내의 바깥 상자는 6px이다. CSS로 그린 아이콘(할 일 체크박스, 자물쇠)과 지도 시트 손잡이는 크기에 맞춘 곡률을 쓰고 이 규칙에서 뺀다.

**한쪽 막대 금지 규칙.** 카드·목록·콜아웃·안내 상자에 1px보다 두꺼운 한쪽 막대를 두지 않는다. 구분이 필요하면 활자, 사방 테두리, 콜아웃의 위 괘선을 쓴다. 한쪽 선은 인용을 표시하는 관례인 인용문(3px)과 목차 레일(2px)에만 남긴다.

주제 점과 지도 노드는 원형이고, 허브와 선택 상태는 링으로 표시한다. 썸네일의 가장자리는 이미지 유형에 따라 그대로 두거나 부드럽게 흐린다.

## Components

컴포넌트는 인쇄물의 도구처럼 조용하게 두고, 상태는 색 대신 판·선·굵기로 알린다.

### 버튼

계열은 넷이고, 상태 변화는 모두 150ms다.

- **채운 버튼:** 페이지의 주요 행동(404의 생각 지도 열기, 연재 허브의 1편부터 읽기, 외부 발행 글의 전문 읽기, 지도 패널의 노트 읽기)에 쓴다. 한 페이지에 하나만 두고, 그 페이지가 독자에게 권하는 일과 같아야 한다. 홈에는 두지 않는다. 홈이 권하는 일은 한 편을 읽는 것이고 그 자리는 대표 글이라, 버튼을 두면 화면에서 가장 강한 신호가 읽기가 아닌 곳을 가리킨다. 높이 44px, 좌우 여백 20px, 글자 15px/600이고, 작은 변형은 높이 40px, 좌우 여백 18px, 글자 14px다. 호버에서 바탕이 보조색으로 바뀐다. 사이트 안으로 가는 버튼에는 화살표를 붙이지 않는다. 채운 버튼은 모양으로 이미 누르면 간다는 뜻이라 화살표가 같은 말을 되풀이한다. 사이트 밖으로 나가는 전문 읽기만 본문 링크와 같은 ↗를 달아 떠난다는 것을 알린다.
- **테두리 버튼:** 도표 크게 보기와 도표 보기 도구(닫기 포함)에 쓴다. 본문 위에 홀로 놓여 경계가 필요하므로 밝은 종이 판에 구분선 색 테두리를 두고, 호버·포커스에서 테두리와 글자가 먹색이 된다.
- **투명 버튼:** 헤더의 검색·공유 아이콘, 책장 필터, 지도 범례와 확대 버튼에 쓴다. 호버에서 호버 판을, 선택에서 먹색 글자와 글자 폭만큼 얹은 2px 먹색 막대를 쓴다. 헤더의 현재 메뉴와 같은 장치다. 막대는 버튼 바닥이 아니라 글자 줄 바로 아래에 두어, 터치에서 버튼을 44px로 키워도 글자에서 떨어져 뜨지 않는다. 호버 판과 구분선 색은 종이색과 대비가 낮아 판으로는 무엇이 눌렸는지 보이지 않는다. 선택된 버튼 안의 개수·접힌 주제 이름은 글자색을 물려받아 함께 먹색이 된다. 헤더 아이콘은 44px 영역에 18px 픽셀 아이콘이고, 지도 확대 버튼은 데스크톱 최소 36px, 모바일 40px, 터치 기기 44px이다.
- **글자 버튼:** 검색 닫기, 지도 시트 닫기, 검색 재시도와 더 보기에 쓴다. 보조색 13px 글자에 링크와 같은 밑줄을 긋고, 누르는 영역은 44px이다.

### 헤더

- **메뉴:** 현재 메뉴는 먹색 글자와, 헤더 아래 선 위에 글자 폭만큼 얹은 2px 먹색 막대로 표시한다. 글자색 차이만으로는 보조색과 2.59:1이라 흐리고, 밑줄은 호버 밑줄과 헷갈려 쓰지 않는다.
- **검색·공유:** 투명 버튼이다. 검색은 접근 가능한 이름을, 공유는 숨긴 텍스트 이름을 둔다. 링크 복사가 성공하면 체크 아이콘과 스크린리더 안내로, 실패하면 버튼 아래에 대안을 담은 짧은 안내로 알린다.
- **두 줄 헤더:** 헤더는 한 줄로 두되, 글자를 키워 한 줄에 들어가지 않으면 첫 줄에 워드마크와 공유·검색을, 둘째 줄에 메뉴를 둔다. 둘째 줄에서도 넘치면 메뉴끼리 줄을 바꾼다. 현재 메뉴 막대는 마지막 줄에 있어 그대로 헤더 아래 선 위에 얹힌다. 검색·공유 버튼과 간격은 글자를 따라 커지지 않으므로, 한 줄에 필요한 폭을 글자 크기로 나눈 값은 글자 100%에서 가장 크다. 그 값을 경계(`19.5em`)로 두면 어느 글자 크기에서든 넘치기 전에 나뉘고, 100%에서는 320px 휴대폰도 한 줄을 지킨다. 메뉴만 가로로 미는 한 줄은 지금 있는 절이 가려질 수 있어 쓰지 않는다.
- **두 줄일 때 고정 해제:** 두 줄 헤더를 화면 위에 붙여 두면 제목으로 건너뛴 자리가 그 아래에 가리고, 큰 글자로 읽는 좁은 화면을 늘 차지하므로 고정을 푼다.

### 목록

- **장부:** 격자를 두 단계로 나눈다. 왼쪽 열에는 연도·분류·등급 라벨을 명조로 크게 두고, 오른쪽의 각 행이 다시 제목·메타와 요약을 좌우로 나눈다. 요약은 세 줄까지 보이고, 720px 이하에서는 두 단계가 모두 세로로 쌓인다. 장부 행의 메타 줄은 구분점 없이 간격으로만 항목을 띄운다. 라벨 칸 62px은 한 글자 등급에 맞춘 값이다. 책장의 세 글자 미분류 묶음만 글자를 키웠을 때 칸이 글자를 따라 넓어지고, 보통 크기에서는 62px을 지켜 다른 묶음과 나란히 선다.
- **개발 노트:** 분류를 절 제목으로 두고, 행의 메타 줄에는 첫 공개 태그와 날짜를 적는다. 태그 순서는 vault 속성 스키마가 정하며 사이트는 고쳐 쓰지 않는다. 첫 태그가 절 제목과 같은 말이면 빌드 로그가 알려 준다.
- **책 표지:** 표지를 불러오지 못하면 표지 크기의 밝은 종이 상자에 구분선 색 테두리를 두고 책 제목을 적는다.

### 리더

- **머리 메타 줄:** 종류·날짜·읽는 시간·주제·원문 링크를 한 줄에 두고 구분점으로 나눈다. 구분점은 앞 항목이 아니라 뒤 항목에 붙여, 좁은 화면에서 줄이 바뀔 때 점만 앞줄 끝에 남지 않게 한다.
- **제목 아래 요약:** 개발 노트의 요약은 제목 바로 아래에 명조 리드문으로 둔다. 인쇄물의 리드문처럼 선이나 판 없이 활자만으로 제목·본문과 구분한다.
- **콜아웃:** 인쇄물의 박스 주석처럼 본문에서 떼어 낸다. 가는 선만으로는 본문의 일부처럼 읽혀서 세 단서를 겹친다. 페이지보다 한 단계 밝은 판, 위쪽 2px 먹색 괘선, 본문보다 작은 `summary` 활자다. 아래쪽은 구분선으로 닫고, 사방 테두리와 한쪽 막대는 두지 않으며, 여백은 `12px 18px 16px`다. 판을 더 짙게 칠하지 않는 이유는 대비다. 짙은 판 위에서는 보조색 머리표가 4.5:1을 넘기 어렵다.
  - 머리표는 `meta` 크기에 굵기 600과 자간 .04em을 준 글자이고, 앞에 종류별 14px 선 아이콘을 둔다. 아이콘은 머리표 색을 따르며, 목록에 없는 종류는 Obsidian처럼 연필을 쓴다. 팁은 화살표, 질문은 물음표 원이다.
  - 요약·인용은 본문을 명조로 흘리고, 경고·위험·실패는 경고색, 버그는 버그색으로 위 괘선과 머리표를 칠한다.
  - 접을 수 있는 콜아웃은 머리표 줄 오른쪽에 꺾쇠를 둔다. `-`는 접힌 채로, `+`는 펼친 채로 시작하고, 꺾쇠는 150ms ease-out(`cubic-bezier(.23, 1, .32, 1)`)으로 돈다. 모바일 목차의 펼침 표시도 같은 꺾쇠다.
  - `hint`·`summary` 같은 별칭은 기본 종류의 모양을 따른다. 종류는 열린 집합이라 이름을 모르는 종류도 같은 구조로 출력하고 CSS가 뜻을 준다.
  - 비교(`compare`)는 본문을 최소 `20rem` 칸의 격자로 두고 안쪽 콜아웃 하나를 한 칸으로 삼는다. 칸에서는 판·괘선·여백·아이콘을 걷어 머리표와 도표만 남긴다. `compare-stacked`는 칸을 한 줄에 하나씩 놓는다.
- **인용문:** 3px 구분선 색 왼쪽 선과 보조색 글자로 표시한다.
- **할 일 목록:** 글머리표 자리에 14px 체크박스를 두고, 1.5px 먹색 테두리와 2px 모서리로 그린다. 완료하면 먹색으로 채우고 바탕색 체크 표시를 넣는다. 공개 사이트에서는 누를 수 없으므로 비활성 상태를 유지하고 포인터 커서·호버 효과를 두지 않는다. 체크박스와 항목 글을 label로 묶어 글이 체크박스의 이름이 되게 하고, 완료한 항목은 그 글에만 보조색과 취소선을 적용해 하위 항목으로 번지지 않게 한다.
- **각주:** 본문의 번호는 먹색 윗첨자 숫자(본문의 0.75배)이고 밑줄이 없다. 글 끝 목록은 사이드바의 참조·역참조와 같은 작은 영역 제목 "각주"(`meta`/600)와 그 아래 1px 선으로 본문과 나누고, 번호와 문장은 본문 바로 아래 단계인 `summary`로 쓴다. 번호를 누르면 번호 아래에 검색 대화상자와 같은 떠 있는 판이 열리고, 판의 글자도 `summary`다. 판은 CSS anchor positioning으로 번호에 붙어 화면 가장자리에서는 안쪽으로 밀리고, 아래 공간이 모자라면 위로 뒤집힌다. 같은 번호, 바깥, Escape로 닫는다. 키보드로 누르면 판 없이 목록으로 가서 ↩로 돌아온다. 화면 낭독기와 키보드 사용자에게는 이 편이 흐름을 잃지 않기 때문이다. 판을 띄우지 못하는 브라우저도 같다.
- **코드 블록:** 강조는 빌드 때 끝내 방문자에게 스크립트를 보내지 않고, 다루지 않는 언어는 원문 그대로 둔다. 문단보다 토큰 종류를 빨리 가려 읽어야 하므로 사이트 톤보다 문법 구분과 대비를 앞세운다. 밝은 화면은 GitHub Light 색에 흰 바탕을 쓴다. 가장 흐린 주석이 종이색 바탕에서는 4.5:1에 조금 못 미치기 때문이다. 어두운 화면은 GitHub Dark 색에 올라온 판을 바탕으로 쓴다. 두 화면 모두 문자열은 테마 기본색 대신 청록으로 두어 숫자·링크의 파랑과 구분하고, 애너테이션은 주석과 갈리도록 함수 색을 쓴다. 인라인 코드는 먹색 8% 판이다. 상자 안 첫 줄은 머리 줄(34px)이고 1px 선으로 코드와 나눈다. 왼쪽에 언어 이름을 보조색 12px로 적고, 오른쪽에 복사 아이콘을 둔다. 언어 이름은 빌드 때 붙이고 `text`나 언어가 없는 블록에는 적지 않는다. 복사 버튼은 스크립트가 있어야 동작하므로 스크립트가 붙이며, 이름 없는 블록의 머리 줄도 그때 만든다. 도표로 바뀌는 블록에는 두지 않는다. 복사 버튼은 44px 영역에 16px 픽셀 아이콘이고, 이름은 "Java 코드 복사"처럼 언어를 넣어 여러 버튼을 구분한다. 44px 판은 머리 줄 밖으로 넘치므로 호버 판과 포커스 윤곽선은 아이콘 둘레 28px에만 그린다. 복사하면 헤더의 링크 복사처럼 2초 동안 체크와 먹색으로 바뀐다. 연속으로 누르면 마지막 요청의 결과만 안내한다.
- **도표:** 본문과 같은 서체로 라벨을 표시하고, 넓은 도표는 내부 스크롤과 크게 보기로 읽으며 키보드로도 조작한다. 노드는 한 가지 면 색(밝은 화면은 연두)으로 두고 색은 묶음이 지게 한다. 노드까지 색을 돌리면 색이 묶음을 뜻하는지 항목을 뜻하는지 가릴 수 없다. 어두운 화면에서는 같은 색상에서 밝기만 다시 고른 짙은 면과 밝은 테두리를 쓰고, 읽는 중에 화면 모드가 바뀌면 원문에서 다시 그린다. 작성자가 색을 지정한 노드는 그 색을 유지한다.
- **테마를 지정한 도표:** 앞머리로 테마를 지정한 도표는 그 테마를 보여 주려는 것이라 사이트 색을 입히지 않고, 그 테마가 전제한 밝기의 판에 화면 모드와 상관없이 둔다. 밝은 테마의 짙은 회색 선은 어두운 상자에서, 어두운 테마의 밝은 글자는 밝은 상자에서 사라지기 때문이다.
- **글 카드:** 본문에서 다른 글로 가는 카드는 1px 구분선 색 사방 테두리와 18px 여백을 쓰고(720px 이하는 14px), 모서리는 본문의 표·코드와 같은 직각이다. 위아래 괘선만 두면 위 선이 절 제목의 밑줄로, 아래 선이 다음 절의 구분선으로 읽혀 카드가 한 덩어리로 묶이지 않는다. 테두리와 여백은 바깥 상자가 아니라 링크에 두어 누르는 자리와 강조되는 영역을 맞추고, 호버 강조는 연재 탐색 카드와 같은 먹색 테두리와 호버 판이다. 키보드 포커스에서는 여기에 전역 윤곽선이 함께 나온다. 연재 탐색 카드만 윤곽선을 끄고 테두리와 판으로 대신한다.
- **연재 탐색:** 이전·다음 편 카드는 1px 구분선 색 테두리와 `14px 18px` 여백을 쓰고, 호버·키보드 포커스에서 먹색 테두리와 호버 판으로 강조한다. 연재 허브 제목 아래의 1편 안내도 같은 이동 안내라 같은 테두리 카드(`16px 18px`)에 채운 버튼을 둔다.
- **연결 탐색:** 참조는 실선, 역참조는 점선이고, 전체 연결은 목록으로도 읽는다. 노드 선택과 키보드 포커스는 링으로, 목차의 현재 위치는 주제색 선과 굵은 글자로 표시한다.
  - 지도 노드는 버튼으로 읽히지만 Enter는 노트를 열고 Space는 지도에 머문 채 고른다. 버튼의 약속과 다르므로 무대에 숨긴 한 줄로 설명한다. 숨긴 줄은 낭독기에만 닿으므로 같은 말을 키보드가 지도에 닿았을 때만(`:focus-visible`) 무대 왼쪽 아래에 드러낸다. 720px 이하에서는 확대 버튼과 겹치지 않게 왼쪽 위에 둔다. 늘 띄우면 마우스로 오는 대다수에게 쓰지 않는 글이 남는다. 허브는 화면에서 링으로만 구분되므로 노드의 접근 가능한 이름 끝에 "허브"를 붙인다. 눌린 상태와 호버 예고편(간선 방향과 나머지 흐림)은 지도에만 둔다. 홈 지도는 누르면 곧바로 노트로 이동해 머무는 선택이 없다.
  - 색면과 영역 이름, 노드 제목 층은 접근성 트리에서 뺀다. 이름은 노드마다 이미 있어서 층까지 읽히면 같은 제목이 두 번 나온다.
  - 노트 사이드바의 블록은 목차를 빼고 모두 위 선으로 시작한다. 목차가 없는 글에서도 첫 블록이 제목 옆에 떠 보이지 않고, 모바일에서는 이 선이 본문과의 경계가 된다.
  - 사이드바의 작은 그래프는 참조·역참조와 같은 블록이라 작은 제목 "연결"과 이웃 수를 단다. 이웃 노드에는 제목을 약 100px 폭 안에서 단어 경계로 두 줄까지 적고, 넘치면 줄임표로 맺는다. 글자 수로 자르면 단어 가운데가 끊기고, 한 줄로는 어떤 생각이 연결됐는지 알기 어렵다. 위쪽 이웃은 제목을 노드 위에, 나머지는 아래에 두어 가운데 노드와 관계선에서 떨어뜨린다. 제목이 읽히도록 이웃은 6개까지만 그리고, 더 있으면 글자를 줄이지 않고 아래 목록으로 안내한다. 전체 제목은 마우스 툴팁과 목록의 같은 줄이 보여 준다. 노드와 목록의 같은 노트 줄은 마우스를 올리거나 포커스하면 함께 강조된다. 노드는 누르는 원에 호버 판을 깔고, 줄은 링크 호버와 같은 밑줄을 긋는다. 강조는 마우스와 키보드 포커스가 모두 그 짝을 벗어났을 때 풀린다.
- **페이지 끝:** 노트 페이지 끝 줄에는 목록으로 돌아가는 링크만 둔다. 사이트 소개 링크는 바닥글에 있다.

### 검색

최대 650px 대화상자에서 입력·로딩·결과·빈 상태·실패를 구분한다. 상태는 결과 목록 위의 한 줄(보조색 13px)이 맡아, 입력 안내와 불러오는 중, 결과 개수, 결과 없음, 실패를 같은 모양으로 두고 문구로 구분한다. 결과를 30개씩 보여 주므로 잘렸을 때는 이 줄이 전체 개수와 보이는 개수를 함께 알리고, 목록 끝의 글자 버튼으로 다음 30개를 더한다. 실패하면 글자 버튼으로 재시도를 제공하고, 단축키 안내는 운영체제에 맞게 표시한다.

입력란은 테두리 없이 머리 줄에 놓지만 색과 포커스 표시는 사이트 규칙을 따른다. 글자는 먹색, 자리 표시 글자는 보조색이고, 포커스는 전역 2px 윤곽선이 맡는다. 브라우저 기본값에 맡기면 어두운 화면에서 자리 표시 글자의 대비가 모자라고, 테두리를 지우면서 포커스 윤곽선까지 함께 꺼진다. iOS는 16px보다 작은 입력란에 초점이 가면 화면을 확대하므로 글자 크기는 모바일에서도 줄이지 않는다.

**한 줄 한 링크 규칙.** 결과 한 줄은 링크 하나로 다룬다. 제목 링크의 `::after`가 줄 전체를 덮어 종류·요약·여백 어디를 눌러도 같은 노트를 연다.

**찾은 말 표시 규칙.** 결과 줄은 왜 걸렸는지 보여야 한다. 제목과 요약에서 찾은 말에 본문의 `==강조==`와 같은 형광을 얹는다. 결과 줄 안에는 본문 강조가 없어 두 표시가 헷갈릴 일이 없다. 형광 위 글자는 `text-highlight`가 선언한 먹색으로 둔다. 보조색을 물려받으면 어두운 화면에서 대비가 모자란다. 요약은 찾은 말 앞에 단어 하나만 남기고 잘라 두 줄까지 보인다. 한 줄에 드는 글자 수는 화면 폭과 글자 크기에 따라 달라서, 앞 문맥을 길게 두면 좁은 화면에서 찾은 말이 두 줄 아래로 밀려 숨는다.

**배경 닫기 규칙.** Escape와 닫기 버튼에 더해 배경을 눌러도 닫는다. 도표 크게 보기와 같은 판정을 쓴다. `dialog`는 배경까지 자기 영역이라 저절로 닫히지 않으므로, 누른 자리가 상자 밖인지 좌표로 가린다. 상자 안에서 눌러 배경에서 손을 뗀 경우는 글자를 고르거나 도표를 끌던 중이므로 닫지 않는다.

### 바닥글과 구분선

- **바닥글:** 모든 페이지 끝에 구분선 아래 한 줄을 두고, 왼쪽에 인장과 사이트 이름을, 오른쪽에 RSS와 사이트 소개 링크를 둔다. 화면 높이에 맞춰 그리는 지도 페이지에는 두지 않는다.
- **구분선:** 정보 덩어리 사이에는 1px 구분선을 쓴다. 목록의 항목 사이와 절 사이를 구분해, 마지막 항목 뒤에는 선을 두지 않는다.

### 아이콘

- **왜 격자로 그리는가:** 1배율 화면에서 가는 선 아이콘은 획이 픽셀 경계에 걸쳐 칠해진 점 대부분이 중간색이 되고, 보조색으로 그리면 그만큼 옅어져 의도한 대비가 남지 않는다. 격자에 맞춰 면으로 그리면 중간색 점이 없어 1배율에서도 획이 또렷하다. 복고 인상은 이 방식의 결과이지 목적이 아니다.
- **두 갈래:** 화면 가장자리에서 누르는 조작 아이콘(헤더의 검색·공유, 코드 블록의 복사와 각각의 완료 표시)은 픽셀 격자로 그리고, 본문 안에서 뜻을 나르는 아이콘(콜아웃 머리표, 참조·역참조 화살표)은 선으로 그린다. 참조·역참조 화살표는 지도 간선의 실선·점선과 짝이라 격자로 바꾸면 목록 제목이 범례 구실을 못 한다.
- **격자:** 조작 아이콘은 쓰이는 크기에 맞춘 격자로 따로 그린다. 헤더는 18칸, 코드 복사는 16칸이고 한 칸이 1px이다. 획은 모두 두 칸이다. 한 칸 획은 18px에서 먼저 흐려진다.
- **칠하는 방법:** 면으로 칠하고 글자색을 따른다(`fill: currentColor`). 선으로 그리면 끝 처리와 굵기 때문에 칸 경계가 흐려져 격자가 사라진다.
- **크기:** 격자 칸 수와 같은 px으로만 보인다. 18칸 도안을 20px으로 키우면 칸마다 굵기가 달라진다. 정수 배율 화면(1배, 2배)에서만 칸이 고르고, 125%나 150% 화면에서는 가장자리가 반 칸씩 번진다. 한 칸 1px 도안의 한계라 고치지 않는다.
- **격자에 담기지 않는 도형:** 테두리가 기호를 감싸는 아이콘은 14칸에 들어가지 않는다. 테두리와 그 안쪽 간격을 빼고 남는 8칸에는 점과 획을 넣을 여유가 없다. 본문 안 아이콘을 선으로 두는 이유다.

### 개인 표식

- **인장:** 21칸 격자를 채운 네모에 이름을 비워 새긴 도장이다. 크기는 격자의 정수배만 쓴다. 정수배가 아닌 크기에서는 칸마다 굵기가 달라져 글자가 뭉개진다.
- **두는 자리:** 바닥글에 21px으로 한 번만 둔다. 한 화면에 같은 표식을 두 번 두지 않고, 글 끝과 본문 안에도 두지 않는다. 읽는 흐름 한가운데에 덩어리가 생기기 때문이다.
- **색:** 인장은 먹색이고 옆의 이름 문구는 바닥글의 보조색을 그대로 쓴다. 비워 새긴 획이 21px에서 2px 굵기라 보조색으로 찍으면 어두운 화면에서 획이 바탕에 묻힌다.
- **읽어 주는 이름:** 인장이 사이트 이름의 첫 어절을 대신하므로 그 어절을 접근 가능한 이름으로 준다. 이름을 대신하지 않는 자리에 두면 화면 낭독기에서 뺀다.
- **파비콘:** 탭과 북마크에서는 인장의 네 글자가 뭉치므로 16칸 격자에 `TZ`만 남긴 도안을 따로 쓰고, 종이색 바탕에 주홍(`#b8432f`)으로 찍는다. 주홍은 팔레트 밖의 색이며 페이지 안에서는 쓰지 않는다. 먹색 한 가지로는 탭 줄에서 다른 아이콘과 구분되지 않기 때문이다. iOS 터치 아이콘은 180px 안에 같은 도안을 10배로 찍고 사방 10px을 여백으로 둔다.

## Do's and Don'ts

아래는 화면에서 확인한 패턴과 앞으로 바꿀 때 지킬 기준이다. 코드가 모두 강제하지는 않으므로 구현과 화면 검토를 함께 대조한다. 그림자와 한쪽 막대는 Elevation & Depth와 Shapes의 규칙을 따른다.

- **Do** 장식보다 제목·본문·참조 관계의 읽기 순서를 우선한다.
- **Do** 색·선·링을 함께 써서 주제와 상태를 표현한다.
- **Don't** 의미 없는 장식 요소, 글로우, 장식용 그라디언트를 더하지 않는다. 개인 표식만 예외로 두되 바닥글과 파비콘에만 정지한 그림으로 둔다. 404의 그림은 없는 페이지가 어떤 상태인지 말하므로 장식이 아니다.
- **Don't** 주제색을 본문 문장이나 메타데이터 장식으로 넓히지 않는다.
- **Don't** 긴 제목·표·연결 목록을 고정 폭으로 두어 화면 밖으로 밀어내지 않는다. 제목은 줄바꿈하고, 표·코드는 안에서 가로로 스크롤하며, 긴 연결 목록은 사이드바 안에서 읽게 한다.
- **Don't** 상태 변화나 사용자 조작을 설명하지 않는 장식용 움직임을 더하지 않는다.

## Motion & Accessibility

**호버 한정 규칙.** 호버 효과는 마우스처럼 포인터를 올려 둘 수 있는 기기에서만 준다. 터치 기기는 탭한 요소에 호버가 남아, 대화상자를 닫은 뒤에도 버튼 판이 깔려 있기 때문이다. 포커스와 선택 상태는 호버와 모양이 같아도 따로 적어 모든 기기에 적용한다.

**44px 규칙.** 누르는 영역은 보이는 크기와 나눠 다루고 44px을 목표로 하되, 이웃을 덮지 않는 것을 먼저 지킨다. 글자와 배치는 그대로 두고, 안쪽 여백과 같은 양의 음수 바깥 여백을 짝지어 영역만 넓힌다.

- 헤더 메뉴는 헤더 높이 전체와 메뉴 간격의 절반까지를 누르는 영역으로 쓰고, 포커스 윤곽선은 글자 둘레에 그린다. "글"처럼 짧은 메뉴는 옆 메뉴와의 간격 때문에 폭을 44px로 넓힐 수 없어, 간격을 포함한 WCAG 2.5.8 최소 기준(24px)을 따른다.
- 목록 행(글·개발 노트 목록, 홈 최근 기록, 외부 발행 글 소개 페이지의 연결 목록)은 제목 링크의 영역을 행 전체로 펼치고, 행 안의 다른 링크는 그 위에 올려 따로 누르게 한다. 위에 올리는 링크는 글자 폭만 차지하게 둔다. 열을 가득 채우면 글자 옆의 빈 자리까지 그 링크가 가져가, 행을 눌렀는데 노트가 아니라 목록이 열린다.
- 지도 범례, 확대 버튼, 책장 필터, 목차와 사이드 목록 줄, 접히는 콜아웃의 머리표, 외부 발행 글 소개 페이지의 원문 링크와 되돌아가기 링크, 책장 끝의 출처 링크는 터치 기기에서만 44px로 키워 마우스 환경의 촘촘한 밀도를 지킨다.
  - 콜아웃 머리표는 콜아웃을 여닫는 유일한 조작이다. 위는 콜아웃의 안쪽 여백이 받아 주고, 아래는 펼쳤을 때 본문이 바로 붙으므로 본문을 넓힌 만큼 물러서게 한다.
  - 장부 행에 올린 발행처 링크는 제목 아래 여백까지만 넓힌다. 더 넓히면 제목 글자를 눌러도 발행처로 간다.
- 헤더 아이콘과 글자 버튼(닫기·재시도·더 보기)은 입력 방식과 상관없이 44px이다.
- 검색 결과 줄은 링크가 줄 전체를 덮어 44px을 넘긴다.
- 넓힌 영역이 이웃한 대상이나 다음 요소에 닿지 않게 한다. 닿으면 나중에 그려지는 쪽이 이겨서 앞의 글자를 눌러도 다른 곳으로 간다. 홈 첫 화면의 보조 링크는 바로 아래가 지도 그림이라 넓힌 만큼 아래 여백을 둔다. 한 덩어리가 여러 줄로 접힐 때는 넓히는 양이 줄 간격의 절반을 넘지 않게 한다. 노트 머리의 메타 줄이 이 규칙을 받는 자리다. 44px을 채우려고 줄 간격을 벌리면 같은 덩어리 안의 간격이 제목까지의 간격보다 넓어져 둘째 줄이 제목에 붙은 것처럼 읽힌다. 그렇게 둔 영역도 WCAG 2.5.8의 24px 기준은 넘는다. 페이지 끝 줄은 링크가 하나뿐이라 44px을 채운다.
- 각주 번호는 좌우 3px만 두어 앞 글자에 붙이고, 누르는 영역은 위아래로만 넓혀 본문 한 줄 높이(약 30px)로 둔다. 가로로 넓히면 붙어 있는 다른 번호나 본문 링크를 덮어 다른 각주가 열린다. 글 끝 목록의 ↩는 줄 끝에 있어 최소 너비 24px과 좌우 4px 안쪽 여백을 실제 배치 공간에 둔다. 둘 다 문장 속 대상이라 WCAG 2.5.8의 예외에 해당한다.

키보드 포커스는 전역 2px 윤곽선과 4px 간격으로 표시하고, 연재 탐색 카드와 그래프는 각자의 테두리와 링을 쓴다. 지도에서 필터나 선택으로 흐려진 노드는 탭 순서에서 뺀다. 남겨 두면 보이지 않는 대상을 수십 번 지나가야 한다. 흐리게 만드는 일은 노드 묶음이 아니라 보이는 점에만 걸어 포커스 링이 함께 흐려지지 않게 한다. 움직임의 길이는 다음과 같다.

- 본문과 조작 요소의 상태 변화: 150ms.
- 누르는 순간 채운 버튼·헤더 아이콘 버튼·지도 확대 버튼이 97%로 줄어드는 반응: 150ms ease-out. 코드 복사 버튼도 같은 비율로 줄지만 전환 없이 바로 줄어든다.
- 지도 시트의 열림과 닫힘: 200ms.
- 그래프의 선택 이동: 320ms.

그래프의 끌기와 확대는 장식용 자동 움직임이 아니라 사용자 입력에 따른 조작이다. 움직임 줄이기 설정에서는 전환과 부드러운 스크롤을 끈다.

## Maintenance & Validation

[공식 명세](https://github.com/google-labs-code/design.md/blob/main/docs/spec.md)의 alpha 형식과 절 순서를 따르고, [설계 철학](https://github.com/google-labs-code/design.md/blob/main/PHILOSOPHY.md)에 맞춰 수치보다 의도를 앞세운다. 핵심 결정은 절마다 짧은 규칙(`**○○ 규칙.**`)으로 적고, 컴포넌트의 다방향 여백은 CSS 표기 그대로 적는다. 새 디자인 결정은 관련 절과 구현을 함께 고치고 Git diff로 검토한다. 결정에 이른 과정은 커밋과 PR에 남기고, 이 문서에는 현재 값과 이유만 둔다.

### 문서와 구현의 대응

| 내용 | 값의 출처 | 일치를 검사하는 테스트 |
|---|---|---|
| 역할 색(밝은·어두운 화면) | `src/lib/palette.ts`, `src/styles/site.css` | `tests/palette.test.ts` |
| 주제색 | `src/lib/format.ts`(화면에는 `var(--topic-…)`로 나감) | `tests/topic-colors.test.ts` |
| 코드 강조색 | `src/styles/body.css` | `tests/code-colors.test.ts` |
| 도표 색과 배치 설정 | `src/scripts/mermaid-config.ts` | `tests/mermaid.test.ts` |
| 글꼴 조각과 적재 | `scripts/vendor-fonts.ts`가 만든 `src/styles/fonts.css`, `public/fonts` | `tests/fonts.test.ts` |
| 대체 서체 목록 | `site.css`의 `--display`·`--sans` | 없음 |
| 글자 크기와 굵기 | `site.css`의 `--t-*` | `tests/typography.test.ts` |
| 페이지 틀 간격과 전환 폭 | `site.css`의 `--s-*` | `tests/spacing.test.ts` |
| 모서리, 전환 시간, 한쪽 막대, 그림자 | 각 CSS와 Astro 스타일 | `tests/shapes.test.ts` |
| 호버 미디어 쿼리, 흐린 노드의 포커스 링, 검색 입력란의 색, 터치 영역의 겹침 | 각 CSS와 Astro 스타일 | `tests/interaction-states.test.ts` |
| 지도의 탭 순서·집계·장식 층, 키 안내와 그것이 드러나는 조건 | `src/graph/engine.ts`, `src/scripts/map.ts`, `src/pages/map/index.astro` | `tests/browser/map.spec.ts` |
| 버튼·링크 공통 모양(`.btn`, `.text-button`) | `src/styles/site.css` | 없음 |
| 본문 모양(콜아웃, 표, 도표 상자) | `src/styles/body.css` | 없음 |
| 본문 안 글 카드의 테두리·여백·강조 | `src/styles/article-card.css` | 없음 |
| 코드 블록 머리 줄과 복사 버튼 | `src/lib/markdown.ts`, `src/scripts/code-copy.ts`, `src/styles/body.css` | `tests/code-block.test.ts` |
| 각주 번호·목록·판 | `src/lib/markdown.ts`, `src/scripts/footnotes.ts`, `src/styles/body.css` | `tests/markdown-footnotes.test.ts`, `tests/footnotes.test.ts` |
| 조작 아이콘의 도안과 표시 크기 | `src/lib/pixel-icons.ts`, `site.css`의 `.search-trigger svg`, `ShareLink.astro`의 `.share-icon`, `body.css`의 `.code-copy-plate svg` | `tests/pixel-icons.test.ts` |
| 개인 표식의 도안·파비콘·터치 아이콘 | `src/lib/mark.ts`, `src/lib/palette.ts`의 `MARK`, `scripts/make-mark.ts`가 만든 `public/favicon.svg`·`apple-touch-icon.png` | `tests/mark.test.ts` |
| 공유 안내의 위치와 폭 | `src/components/ShareLink.astro` | 없음 |
| 공유 안내가 5초 뒤 사라지는 동작 | `src/scripts/share.ts` | `tests/share.test.ts` |
| 사이드바 작은 그래프의 제목과 목록의 함께 강조 | `src/components/local-graph-layout.ts`, `src/components/LocalGraph.astro`, `src/scripts/local-graph.ts`, `src/components/NoteSidebar.astro` | `tests/local-graph.test.ts`, `tests/local-graph-links.test.ts`, `tests/browser/reader.spec.ts` |
| 폭에 따른 리더·지도 배치 | `src/components/NotePage.astro`, `src/pages/map/index.astro` | 없음 |
| 홈 지도의 폭·입력 방식에 따른 전환(살아 있는 지도와 정적 그림) | `src/scripts/hero-graph.ts`, `src/pages/index.astro` | `tests/hero-graph.test.ts`, `tests/browser/reader.spec.ts` |
| 외부 발행 글의 목록 복귀 링크와 앞 목록의 클릭 영역 | `src/components/ExternalArticle.astro` | `tests/browser/reader.spec.ts` |
| 지도와 홈 지도가 나누는 호버 예고편·누름 상태 | `src/graph/engine.ts` | `tests/browser/map.spec.ts`, `tests/browser/home.spec.ts` |
| 지도 시트로 바뀌는 폭과 그 폭의 대화상자 처리 | `src/components/MapPanel.astro`, `src/scripts/map.ts`, `src/pages/map/index.astro` | `tests/interaction-states.test.ts`, `tests/browser/map.spec.ts` |
| 검색 결과 줄의 형광과 두 줄 요약 | `src/lib/search-match.ts`, `src/scripts/search.ts`, `src/styles/site.css` | `tests/search-match.test.ts`, `tests/browser/search.spec.ts` |
| 검색·도표 크게 보기의 배경 닫기와 끌기 보호 | `src/scripts/dialog-backdrop.ts` | `tests/dialog-backdrop.test.ts`, `tests/browser/search.spec.ts` |
| 글자를 키웠을 때 책장 라벨 칸이 넓어지는 조건 | `src/pages/books/index.astro` | `tests/browser/books.spec.ts` |
| 글자를 키웠을 때 헤더가 두 줄로 나뉘는 경계와 고정 해제 | `src/styles/site.css` | `tests/browser/header.spec.ts` |
| 홈 첫 화면과 최근 기록의 누르는 영역 | `src/components/About.astro`, `src/pages/index.astro` | `tests/browser/home.spec.ts` |

### 검사

```bash
npm run design:lint
```

lint는 문서 구조, 토큰 참조, 컴포넌트에 선언한 바탕·글자 색의 대비를 검사하고, 쓰이지 않는 색 토큰에 경고한다. 문서와 구현 코드의 값이 같은지는 위 표의 테스트가 맡는다. 브라우저 검사는 실제 배치와 입력이 있어야 알 수 있는 흐름을 확인한다. 실제 화면 낭독, 포커스의 식별성과 아이콘·선의 비텍스트 대비는 별도로 확인한다. 토큰 변경이 클 때는 같은 버전의 `designmd diff <이전 파일> DESIGN.md`를 함께 쓴다.

화면 변경은 홈·긴 노트·목록·지도·검색을 320px, 390px, 1440px와 전환 폭 720px·1000px 전후에서, 밝은 화면과 어두운 화면 모두 확인한다. 글자 200%도 확인한다. 긴 제목, 썸네일 유무, 검색·공유 실패, 키보드 탐색을 포함한다. 문서 검사와 화면 검증의 결과는 나눠 기록한다. 프로젝트 명령은 [AGENTS.md](AGENTS.md), 콘텐츠 표시 규칙은 [AUTHORING.md](AUTHORING.md)가 관리한다.
